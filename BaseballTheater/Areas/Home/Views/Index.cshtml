@using System.Web.Optimization
@using BaseballTheater.Areas.Home

@{
	Layout = "~/Shared/Views/_Layout.cshtml";
	ViewBag.BodyClass += " Home ";
}

@{ 
	Html.RenderPartial("Templates/_GameSummary");
	Html.RenderPartial("Templates/_GameDetail");
}

<div class="settings flex">
	<div class="day-nav">
		<a v-bind:href="getUrlDayBefore(date)">
			<i class="fa fa-chevron-circle-left"></i>
		</a>
		<input type="text" id="calendarpicker" v-bind:value="getFriendlyDate(date)" v-bind:size="getFriendlyDate(date).length + 1" />
		<i class="fa fa-calendar" aria-hidden="true"></i>
		<a v-bind:href="getUrlDayAfter(date)">
			<i class="fa fa-chevron-circle-right"></i>
		</a>
	</div>

	<div class="tools flex">
		<select id="favorite-team">
			<option>- Select Favorite Team -</option>
			
			<option v-for="team in teams" v-bind:value="team">{{teams[team]}}</option>
		</select>
		<div class="hide-scores">
			<input type="checkbox" name="scoreshidden" id="scoreshidden" />
			<label for="scoreshidden">Hide Scores</label>
		</div>
	</div>
</div>

<div class="game-list" v-if="gameSummaries">
	<game-summary v-for="game in gameSummaries" v-bind:game="game">
		
	</game-summary>
</div>

<div class="highlights" v-if="highlights">
	<highlight v-for="highlight in highlights" v-bind:highlight="highlight">
		
	</highlight>
</div>